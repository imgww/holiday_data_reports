# ==============================================
# 白名单模式：每次 git add 只提交三类采集成果
#   - 各目录下的 holiday-data-fetch.json
#   - 各目录下的 采集日志.csv
#   - 各目录下的 snapshots/ 快照
# 其余（data_R*.json、round_*.json、脚本等）一律忽略
# ==============================================

# 1. 先忽略所有文件和目录
*
# 2. 放行所有目录本身（否则 git 无法进入被忽略目录，后续放行规则失效）
!*/
# 3. 放行任意层级的两个采集成果文件（无斜杠的模式即匹配任意层级）
!holiday-data-fetch.json
!采集日志.csv
# 4. 放行任意层级的 snapshots 目录及其内部全部内容
!**/snapshots/
!**/snapshots/**
# 5. 排除 2019~2022 年份数据（不入库，仅保留 2023-2026）
/2019_*/
/2020_*/
/2021_*/
/2022_*/
# 6. 放行本配置文件自身（可选：让规则可随仓库提交；不需要可删掉此行）
!.gitignore